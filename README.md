# Telegram Bot для работы с Excel и Mistral AI

Telegram бот для загрузки Excel файлов, сохранения их в JSON БД, и взаимодействия с данными через Mistral AI.

## Возможности

- 📁 **Загрузка Excel файлов** - бот принимает .xlsx/.xls файлы и сохраняет их в JSON БД
- 💬 **Вопросы к данным** - задавайте вопросы о данных, Mistral AI отвечает на основе БД
- ✏️ **Редактирование данных** - Mistral может редактировать данные по вашему запросу
- 📊 **Экспорт в Excel** - автоматический экспорт обновленных данных в Excel файлы

## Установка

1. Клонируйте репозиторий или скачайте файлы

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

4. Заполните `.env` файл:
   - `TELEGRAM_BOT_TOKEN` - получите у [@BotFather](https://t.me/BotFather) в Telegram
   - `MISTRAL_API_KEY` - получите на [console.mistral.ai](https://console.mistral.ai/)

## Запуск

```bash
python bot.py
```

## Использование

### Команды бота:
- `/start` - приветственное сообщение
- `/help` - справка по использованию
- `/status` - статус базы данных

### Работа с ботом:

1. **Загрузка Excel файла:**
   - Просто отправьте файл .xlsx или .xls боту
   - Данные будут автоматически прочитаны и сохранены в БД

2. **Вопросы к данным:**
   - Напишите любой вопрос о данных
   - Пример: "Сколько строк в базе данных?"
   - Пример: "Покажи все уникальные значения в колонке 'Город'"

3. **Редактирование данных:**
   - Попросите Mistral изменить данные
   - Пример: "Измени значение в строке 5, колонке 'Имя' на 'Иван'"
   - Бот автоматически обновит БД

4. **Экспорт данных:**
   - Попросите экспортировать данные
   - Пример: "Экспортируй лист 'Лист1' в Excel"
   - Бот создаст и отправит Excel файл

## Структура проекта

```
.
├── bot.py              # Основной файл бота
├── config.py           # Конфигурация
├── excel_handler.py    # Работа с Excel файлами
├── json_db.py          # Работа с JSON БД
├── mistral_ai.py       # Интеграция с Mistral AI
├── requirements.txt    # Зависимости
├── .env.example        # Пример файла окружения
├── database.json       # База данных (создается автоматически)
├── uploads/            # Временные файлы загрузок
└── exports/            # Экспортированные файлы
```

## Технологии

- `python-telegram-bot` - Telegram Bot API
- `pandas` - работа с Excel файлами
- `openpyxl` - чтение/запись Excel
- `mistralai` - интеграция с Mistral AI
- `aiofiles` - асинхронная работа с файлами

## Примечания

- БД хранится в файле `database.json`
- Все данные из Excel сохраняются с сохранением структуры листов
- Mistral AI использует JSON БД как контекст для ответов
- При редактировании данных через Mistral, изменения сохраняются автоматически

